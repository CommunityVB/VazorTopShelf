@{
  // Since we're hooking into the ASP.NET pipeline from the HomeController's
  // Index action and injecting the Index.cshtml.vb Vazor view from there,
  // any code or markup in this *.cshtml view is ignored and has no effect.
}
